{
  "name": "@northpoint/db",
  "version": "0.0.0",
  "private": true,
  "description": "Drizzle schema, migrations, and DB access layer (populated in Group B).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "echo 'no lint yet'",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .turbo"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
