# Copy this file to .env.local and fill in real values from the Supabase dashboard.
# .env.local is gitignored — never commit real credentials.

# ---------- Supabase ----------
# Public (browser-safe). Found in Supabase dashboard → Project Settings → API Keys.
# The value is the `sb_publishable_…` key (Supabase's current naming).
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_your-publishable-key

# Server-only. Bypasses Row-Level Security. NEVER expose to the browser.
# The value is the `sb_secret_…` key (Supabase's current naming).
SUPABASE_SECRET_KEY=sb_secret_your-secret-key

# Direct Postgres connection. Used by Drizzle (Group B+). Found in
# Supabase dashboard → Project Settings → Database → Connection string (URI).
DATABASE_URL=postgresql://postgres:[password]@db.your-project-ref.supabase.co:5432/postgres
